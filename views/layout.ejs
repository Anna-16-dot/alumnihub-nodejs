<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0, user-scalable=yes, viewport-fit=cover">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="default">
    <meta name="theme-color" content="#4F46E5">
    <meta name="msapplication-TileColor" content="#4F46E5">
    <meta name="format-detection" content="telephone=no">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title><%= typeof pageTitle !== 'undefined' ? pageTitle : 'Dashboard' %> - <%= typeof APP_NAME !== 'undefined' ? APP_NAME : 'VCET Alumni Hub' %></title>
    
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" crossorigin="anonymous">
    <!-- Custom Mobile CSS (with cache bust) -->
    <link rel="stylesheet" href="/css/mobile.css?v=9.0">
</head>
<body class="<%= typeof isLoggedIn !== 'undefined' && isLoggedIn ? 'logged-in' : 'logged-out' %>">
    <% if (typeof isLoggedIn !== 'undefined' && isLoggedIn) { %>
        <%- include('partials/navbar') %>
        <div class="content-wrapper">
            <%- include('partials/flash') %>
            <%- body %>
        </div>
        <%- include('partials/bottom-nav') %>
    <% } else { %>
        <%- body %>
    <% } %>

    <!-- Bootstrap JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
    <!-- Custom JS -->
    <script src="/js/app.js"></script>
</body>
</html>
