<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0, user-scalable=yes, viewport-fit=cover">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="default">
    <meta name="theme-color" content="#667eea">
    <meta name="msapplication-TileColor" content="#667eea">
    <meta name="format-detection" content="telephone=no">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title><%= pageTitle %> - <%= APP_NAME %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/mobile.css?v=9.0">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%) !important;
            min-height: 100vh !important;
            display: flex !important;
            align-items: center !important;
            justify-content: center !important;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif !important;
            overflow: auto !important;
        }
        .landing-container {
            text-align: center;
            color: white;
            padding: 2rem;
            z-index: 10;
            position: relative;
        }
        .landing-icon {
            font-size: 5rem;
            margin-bottom: 1rem;
            animation: bounce 2s infinite;
            display: inline-block;
        }
        @keyframes bounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-20px); }
        }
        .btn-custom {
            padding: 0.75rem 2rem;
            font-size: 1.1rem;
            border-radius: 50px;
            margin: 0.5rem;
            min-width: 200px;
            display: inline-block;
        }
    </style>
</head>
<body>
    <div class="landing-container">
        <i class="bi bi-mortarboard-fill landing-icon"></i>
        <h1 class="display-4 fw-bold mb-3">VCET Alumni Hub</h1>
        <p class="lead mb-4">Connect with alumni, explore opportunities, and grow your network</p>
        
        <div class="d-flex flex-column align-items-center">
            <a href="/auth/login" class="btn btn-light btn-custom">
                <i class="bi bi-box-arrow-in-right me-2"></i>Login
            </a>
            <a href="/auth/register" class="btn btn-outline-light btn-custom">
                <i class="bi bi-person-plus me-2"></i>Register
            </a>
        </div>

        <div class="mt-5">
            <small class="text-white-50">Made with ❤️ for VCET Community</small>
        </div>
    </div>
</body>
</html>
